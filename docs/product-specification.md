# EventSpace 第一版产品规格

> 状态：2026-07-30 当前实现按 Host-led 封闭 MVP 校准；所有商业数值保留配置化空间。
> 范围：移动端优先的私密临时活动空间；桌面端适配；首发英文界面。  
> 参考：完整逐项决策见 `requirements-baseline.md`。

## 1. 产品与边界

EventSpace 让活动组织者创建一个私密、限时的 Host-led 活动房间。当前实现包含聊天、Photos 和独立行程三项核心能力；房间结束时由服务器立即冻结并进入只读归档路径。Book 回忆录、Community-led / 无 Host 模式和投票均是历史或延期方向，不属于当前封闭 MVP。

第一版不提供：公开房间发现流、陌生人私信、全局个人主页、广告、第三方行为追踪、实时位置共享、图片滤镜、回忆录内容投票、多选/排序投票、站内分摊付款或严格端到端加密。

## 2. 房间生命周期

1. 已登录创建者建立 Host-led 房间，设置名称、可选说明、时长、人数上限和入口/审核方式。
2. 活动期内成员按权限进行聊天、Photos 和行程协作。
3. 房间可因服务器到期或 Host 手动结束而冻结。服务器时间是唯一准则；到期无缓冲，所有写操作立即拒绝。
4. 界面先显示“正在归档”。一般数秒完成；媒体较多时后台完成，完成后通知成员。
5. 归档只读，聊天、Photos 和行程均可查看。照片可以保存到本地；语音可播放但不提供下载按钮。

免费房间最短 15 分钟、最长 24 小时；付费房间最长 7 天。单次最多延长 24 小时，不能缩短到当前时刻之前。当前 MVP 不提供投票延长。

## 3. 角色、权限与模式

| 角色/模式 | 核心规则 |
| --- | --- |
| Host | 可结束及修改房间时长、转让 Host、管理管理员、入房、治理与内容。 |
| 管理员 | 几乎拥有 Host 的治理能力，但不能结束、延长或缩短房间。 |
| 已登录成员 | 可发送文字/短语音、添加 Photos、评论和表情回应；仅删除自己的内容。 |
| 访客 | 通过 Supabase anonymous sign-in 获得临时身份；必须提供昵称，未登录时使用首字母头像。可发送基础文字/短语音。 |
| Host 房间 | Host/管理员决定一般管理、成员审核与行程创建；当前不提供 Community-led 投票治理。 |

Host 只能转让给房间内已登录成员，接收者确认后生效；原 Host 自动成为管理员。管理员数量不设固定上限，但添加时须显示权限风险提醒。

踢出只将成员移出当前房间；若未被拉黑且入口仍有效，对方可重新申请。拉黑则禁止再次进入。成员被踢出、拉黑或主动离开后立即失去未来访问权；已经看到、复制或下载的内容无法远程撤销。

## 4. 进入、身份与成员资格

- 房间默认私密，不被公开发现。
- 支持链接、可扫描链接二维码和邀请码；创建者可设为持链接直接进入、仍需邀请码或必须审核。
- Host 可作废邀请码、轮换链接/二维码。已进入成员保持访问权；新成员必须使用新入口。
- 审核申请展示头像、昵称和申请状态。Host/管理员处理通过或拒绝；当前实现不采集申请备注，也不触发成员投票。
- 成员名单按 Host-led 房间规则展示；当前不提供无 Host 成员名单策略。
- 地理位置限制不在第一版范围内。
- 创建房间必须登录；登录支持 Google 与无密码邮箱 magic link/OTP。
- 已登录用户可上传账号头像，加入房间时默认带入头像；未登录访客当前使用昵称首字母。房间内昵称必须唯一。
- 访客关闭浏览器、清除站点数据或换设备后临时身份失效。登录用户可跨设备恢复身份。

归档只对未被踢出的原成员开放，且需要登录。访客若在结束后才登录，只要当前设备仍保存该房间身份，即可绑定个人归档。

## 5. 聊天

- 支持文字、短语音、@ 提及、回复、撤回、置顶和本房间搜索。
- 当前 Chat 不再创建图片或位置消息；历史消息若存在媒体类型，可兼容展示。Photos 是正式图片入口。
- 语音最长 60 秒，支持进度条拖动；不支持转文字或倍速；默认不自动播放。
- 用户可在两分钟内无痕撤回自己的消息；消息不支持编辑。
- Host/管理员可无痕删除其他成员消息；置顶仅限 Host/管理员。
- 输入规则：Enter 发送，Shift+Enter 换行；移动键盘 Enter 同样发送，换行由明确的输入控件完成。
- 不显示在线成员、输入状态或已读回执。
- 消息与评论最长 2,000 字符，支持 Unicode 与 emoji。

系统消息记录成员加入/离开、Host 转让、管理员变更、行程变化及房间即将结束；当前 MVP 不产生投票系统消息。

## 6. Photos

当前 Photos 以照片网格组织内容。已登录成员可添加照片和评论；本人可删除自己的照片，Host/管理员可删除他人照片。Book、双页 spread 与文本编排不属于当前实现。

- Photos 使用紧凑网格和全屏详情层；详情包含大图、作者、评论和有权限时的删除操作。
- 上传来源当前为设备文件选择；不提供相机捕获、Chat 内容导入、caption 层、纸张样式、双页 spread 或复杂自由排版。
- 新照片写入前必须完成 Storage 上传、asset finalize 和 `photos` 记录创建；失败时 UI 回滚并显示错误。

照片支持 Android/iOS 常用格式与 Live/Motion Photo；不兼容动态照片时转为静态主图并告知用户。原始文件最大 10 MB；上传后去除 EXIF，压缩至最长边 2560px、约 85 质量，不保留原图。移动端支持拍照/相册，桌面支持拖拽上传。

## 7. 行程

行程为独立页面，围绕活动时间线展示开始/结束时间、地点、说明、地图和负责人，不承担成员签到或报名。

- 地点支持地图搜索与手动文字输入；展示地点卡片并跳转 Google Maps/Apple Maps，不做路线或实时位置共享。
- 状态仅为未开始、进行中、已结束。定时行程由开始/结束时间自动推导；创建者也可选择手动结束模式，开始后由负责人、Host 或管理员明确结束，不提供任意状态推进或回退。
- Host 房间仅 Host/管理员可创建行程并指定负责人；负责人、Host、管理员可编辑或删除。当前没有行程投票提案。
- 负责人可为访客，以“临时身份 + 当前设备”绑定。
- 创建者设置活动时区（默认设备时区）；数据以 UTC 保存，成员同时看到本地时间和活动时区时间；夏令时自动遵循活动所在地规则。
- 页面按状态形成未来到过去的纵向顺序：未开始在上、进行中居中、已结束在下；每次进入优先滚动到正在进行的行程，没有进行中行程时滚动到最近下一项或最近结束项。

## 8. 投票

投票系统延期。旧 mock reducer/type 和历史文档只作为兼容记录，不进入当前 UI、生产 schema 或封闭 MVP 验收。未来若恢复投票，必须重新定义固定投票分母、匿名/公开、截止、幂等和服务端结算规则。

## 9. 治理与反滥用

- 每位用户可私密举报。举报人描述违规者及内容位置，Host 私密回复；举报不自动转交原始内容。
- 禁言者仍可浏览房间和行程，但不能发文字/语音/@、评论/表情或照片。
- 设备级拉黑使用随机本地设备令牌，不做浏览器指纹；清除站点数据后令牌失效。
- 访客有文字、@ 和语音频率限制；异常时要求简易人机验证。
- 所有写入、权限和到期校验都在服务端执行。

## 10. 归档、账户与付费

归档为个人列表入口：成员只能从自己的列表移除，无法影响其他成员。免费归档保留 30 天；到期前 7 天提醒成员，主存储在到期后 7 天内删除，备份再于 30 天内清理。

用户删除账户时，显示名变为 `Deleted member`、头像移除；内容保留至普通归档到期。永久归档内容保留，但不再关联该账户。

第一版不做个人订阅，所有付费均附着于房间并惠及全部符合归档资格的成员：

- 免费：最多 10 名成员、25 张照片、250 MB 媒体、最长 24 小时、归档 30 天。
- 付费活动扩展：暂定最多 50 名成员、200 张照片、1 GB 媒体、最长 7 天；数值必须配置化。
- 永久归档：独立的一次性房间级项目，与活动扩展分开售卖；按购买时房间容量保存，后续可购买容量扩展包。
- 房间可创建时付费，或在运行中/免费归档未过期时由任一已登录成员升级。首发不做站内分摊。
- 永久归档生效后不退款；支付失败不解锁；付款人删号不影响其他原成员。
- 支付使用 Stripe 托管信用卡支付与基础税务能力，产品不存储支付卡资料。

## 11. 页面与体验

- 房间使用两层固定顶部结构：第一层左侧为返回 Rooms 的图标、中间为严格居中的房间名称/状态、右侧为必要操作；第二层常驻 `Chat / Photos / Itinerary`。
- 桌面端：聊天页为消息主区与房间信息栏；Photos 使用照片网格与全屏详情；行程页为内容区与详情栏。
- 固定房间栏展示名称、倒计时、状态、分享、成员和更多操作，不展示在线人数或付费入口。
- 登录后的首页直接从进行中/已归档切换与房间卡片开始，不显示营销式标题或欢迎语；支持收藏、搜索和筛选。卡片当前展示 Photos 预览、名称、状态、结束时间和成员数。
- 公开首页只提供产品说明、创建、通过链接进入、登录、条款与隐私入口；没有公开房间。
- 网络断线时保留文字草稿；Photos 或行程写操作失败时按稳定命令重试或明确告知失败。若权限、版本或状态已改变必须明确告知失败。
- 动效是核心体验：120Hz 设备尽可能接近 120fps，一般设备至少流畅 60fps；性能不足时优先输入、拖动和可读性。遵循“减少动态效果”、键盘操作及色彩对比要求。

首发承诺支持当前及前一主要版本的 Chrome、Edge、Safari（macOS/iOS/iPadOS）；Firefox 不作为首发承诺浏览器。

## 12. 法律与用户支持

- 面向美国、欧盟与澳大利亚的英语用户；最低年龄 16 岁。
- 注册或创建房间须勾选“我已满 16 岁并同意服务条款与隐私政策”，不收集生日。
- 第一版提供支持邮箱或联系表单、服务条款、隐私政策、社区规则、Cookie 说明页面。
- 正式上线前，以上法律文本及地区性隐私/消费者权益流程必须由目标法域律师审阅。

## 2026-07-30 当前同步：封闭 MVP 产品范围

- 当前正式能力已经不是纯本地 Mock：创建、邀请、加入、账号、Room、Chat、Photos、Itinerary 和成员治理均有真实 Supabase 接线路径。
- 邀请二维码现在编码真实 invite URL；复制链接、短码和下载 PNG 都应被视为可分享入口，不能再称为视觉 mock。
- 访客加入不强制先登录邮箱；未登录提交会创建 Supabase anonymous session。匿名访客仍不是永久账号，跨设备恢复、匿名清理和账号认领仍需后续生产策略。
- 账号头像属于真实私有媒体能力的一部分：仅已登录非 anonymous 账号可上传；头像可被带入房间成员、审核申请、Chat、Itinerary 和 Rooms 入口。
- 当前加入页只收集昵称并展示头像上下文；申请备注在代码中为空字符串，若产品要恢复备注，需要同步 UI、RPC、测试和文档。
- Community-led、投票、Book、Chat 图片发送、相机入口、caption 和复杂回忆录排版均不是当前 MVP 能力。
## 2026-07-18 当前同步：产品规格与现状差异

本节为历史同步，保留用于理解旧本地优先阶段；当前判断以 2026-07-30 同步为准。

### 已经在本地版本中具备的产品能力

- Rooms：All / Active / Achieved / Favorite 筛选、搜索、收藏、删除个人入口、移动端单卡横滑与网格切换；当前仍为旧 Board snapshot，回忆录卡片预览待迁移。
- Create Room：三步创建、移动端时间滚轮、本地账号门槛、创建成功邀请卡片、mock QR / 邀请码、Open this room。
- Chat：底部输入栏、右侧自己消息、发送后滚动到底部、搜索、回复、反应、撤回、置顶、管理员删除、语音条 mock。
- Poll：yes/no、选项、行程投票；open minutes；匿名/公开；结果进度条；投票历史全屏查看。
- Photos：本机多选上传、压缩、本地 Blob 保存、照片网格、照片详情、评论与本人/管理员删除；当前没有相机入口、Chat 内容导入、Book、caption 层或纸张样式。
- Itinerary：负责人、起止时间、地点、说明、时间自动状态、冲突提示与外部地图入口；行程投票通过后可添加计划。
- Governance：Host/Admin 与 Community-led 的基础差异、成员审核、禁言、移除、拉黑、入房多数表决、房间结束和归档 mock。

### 当前仍是产品目标、未进入生产实现的能力

- 真实登录、匿名访客认领、跨设备身份、成员邀请发送。
- 多人实时同步、消息送达、冲突处理、服务端裁决。
- 私有媒体存储、EXIF 清理、转码、缩略图、签名 URL。
- 生产级语音上传/转码/跨设备播放、推送、邮件、PWA 离线、支付和永久归档。
- 正式法律文本、用户支持流程、审计日志和反滥用系统。

### 产品规则提醒

当前 UI 已经验证了大量移动端交互，但后端阶段不得把本地 reducer 的结果视为可信事实。房间到期、投票通过、成员资格、内容所有权和归档资格都必须由服务端重新判定。

## 2026-07-19 当前同步：核心体验落地说明

- Chat 的第一版本地体验确定为轻量 Telegram 式操作：消息分组、回复、长按操作、附件托盘、照片、位置、按住录音、置顶和回到最新；不扩展频道、机器人、转发网络、已读回执或复杂联系人体系。
- 旧 Board/Sequence 产品方向自 `13a4102` 起被 Photos/Book 回忆录取代；旧画布代码仅作为迁移期间的复用来源，不再定义正式 Room 体验。
- `/rooms/new` 时长仍遵守既有 15 分钟至 24 小时规则；循环滚轮只是移动端选择方式变化，不改变产品配额。
- Host-led 的 Member list 可在 Moderators / Everyone 间选择；Community-led 仍固定 Everyone。此次仅修正控件层级，没有改变权限规则。
- Rooms 卡片后续必须迁移为回忆录封面或 Photos spread 预览；当前仍展示旧 Board snapshot，属于已知阶段差异。

## 2026-07-23 历史同步：回忆录第一阶段边界

- 本地版本已经建立稳定双页、纸张样式、内容页归属、照片 caption 和 Book 阅读体验。
- 复杂排版引擎尚未完成；当前不宣称支持跨页拖动、任意布局、图层、裁切或多人并发排版。
- `boardItems` / `boardComments` 仍作为兼容存储字段。生产后端应采用 memoir/page/item/caption 语义的独立 DTO，而不是照搬当前字段名。
